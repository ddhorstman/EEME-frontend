import React from "react";
import RenderLinkShortener from "./RenderLinkShortener";

interface Props {}

const LinkShortenerSubmission: React.FC<Props> = () => {
  return <RenderLinkShortener />;
};

export default LinkShortenerSubmission;
